# Client-confidential material — never commit or host (contains the Dubai client's
# live game screenshots with visible usernames). Kept local only.
ref/

# Local-only working notes (not for the public repo)
CLAUDE.md

# Signing keystores — never commit (kept as CI secrets only)
*.jks
*.keystore
*.b64

# Android build output
android/.gradle/
android/build/
android/app/build/
android/local.properties

# OS / editor junk
Thumbs.db
desktop.ini
.DS_Store
*.swp
.vscode/
.idea/
